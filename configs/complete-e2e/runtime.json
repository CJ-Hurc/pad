{
  "schema": "hurc-complete-e2e-runtime/v1",
  "description": "CLI adapter so pad consumer compile surfaces the prove entry.",
  "adapters": [
    {
      "id": "pad-consumer-cli",
      "kind": "cli",
      "surface_kind": "cli",
      "timeout_sec": 180,
      "argv": ["python3", "scripts/complete-e2e/list-surfaces.py"]
    }
  ]
}
